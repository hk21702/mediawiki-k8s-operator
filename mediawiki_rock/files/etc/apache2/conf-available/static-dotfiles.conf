# Block access to dot files/directories (e.g. .git) under /static.
# This prevents exposure of VCS metadata from git-sync checkouts.
<LocationMatch "^/static/(.+/)?\."> 
	Require all denied
</LocationMatch>
